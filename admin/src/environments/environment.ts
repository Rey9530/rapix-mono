// Configuración para producción. La versión de desarrollo se carga
// automáticamente vía `fileReplacements` de angular.json.
export const environment = {
  produccion: true,
  urlApi: "/api/v1",
  tokenMapboxPublico: "",
};
